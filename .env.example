# Firebase web app config for project `drivingo-app`.
# Values from Firebase console → Project settings → Your apps → Web app.
# Not secrets, but kept out of the repo so builds stay environment-agnostic.
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=drivingo-app
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
